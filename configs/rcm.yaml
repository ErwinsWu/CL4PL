dataset:
  dataset_name: RadioMap
  dataset: /Users/erwinswu/radioMap/Boston/
  batch_size: 8
  num_workers: 1
  train_ratio: 0.7
  test_ratio: 0.2

model:
# ['DCLPA', 'EWC', 'MAS', 'LWF', 'ILT', 'RCM', 'MDIL', 'VANILLA', 'DEFORMABLE']
  model_name: 'RCM'
  save_dir: checkpoints/rcm/
  tasks: ['USC','Boston','UCLA','SH']
  task: Boston
  # new_save_dir: checkpoints/DCLPA/

training:
  num_epochs: 120 
  lr: 0.0001
  seed: 42
  train_flag: False
  continue_train: False
  last_epoch: 15
  best_val_loss: 0.0334
  # ['MSE', 'KL', 'COS']
  loss: MSE
  checkpoint_path: checkpoints/rcm/

logs:
  train_logger_path: logs/rcm/
  test_logger_path: logs/rcm/


evaluation:
  model_path: checkpoints/rcm/


